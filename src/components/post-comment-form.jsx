export default function() {
    const { currentUser, setCurrentUser } = useContext(CurrentUserContext);

    return (
        <>
            <form>
                <label></label>
            </form>
        </>
    )
}